<nav class="navbar navbar-inverse navbar-top navbar-fixed-top">
  <div class="container-fluid">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <div id="navbar" class="navbar-collapse collapse">
          <a class="navbar-brand" href="/">MetLand</a>
          <ul class="nav navbar-nav">
            <li class="nav-item active">
              <a class="nav-link" href="/"><%= t('global.menu.home')%><span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/dailies"><%= t('global.menu.data')%></a>
            </li>
            <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to t('global.menu.profile'), edit_user_registration_path %>
            </li>
            <% else %>
            <li class="nav-item">
              <a class="nav-link" href="/users/sign_up"><i class="fa fa-user"></i><%= t('global.menu.sign_up')%></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/users/sign_in"><i class="fa fa-sign-in"></i><%= t('global.menu.log_in')%></a>
            </li>
            <% end %>
            <% if user_signed_in? %>
              <li role="separator" class="divider"></li>
              <li><%= link_to t('global.menu.log_out'), destroy_user_session_path, method: :delete %></li>
            <% end %>
            <li class="nav-item">
            <% if I18n.locale == I18n.default_locale %>
              <%= link_to "English", { :locale=>'en' } %>
            <% else %>
              <%= link_to "Magyar", { :locale=>'hu' } %>
            <%end%>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>